<!doctype html>
<html lang="en">
  <head>
    {% render "head", metadata: site.metadata, title: page.title %}
  </head>
  <body class="{{ page.layout }} {{ page.page_class }} min-h-screen flex flex-col">
    <main class="flex-1">
      {{ content }}
    </main>

    {% render "newsletter", metadata: site.metadata %}
    {% render "footer", metadata: site.metadata %}
  </body>
</html>
